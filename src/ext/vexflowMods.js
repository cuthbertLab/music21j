define(['vexflow'], function(Vex) {    
    // not used anymore, but can substitute for Vex.Flow if there's something that can't
    // be fixed otherwise...
    
    // Vex.Flow.Beams = ... e.g.,
    return Vex;
});
